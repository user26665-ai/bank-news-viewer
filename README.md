# 🏦 Банковский новостной агент

AI агент для анализа финансовых новостей с приоритезацией критически важной информации для банков.

## ✨ Особенности

- 🎯 **Приоритезация для банка**: Санкции, ЦБ, валюта (x3 буст)
- 🚫 **Фильтрация нерелевантного**: Спорт, шоу-бизнес (x0.1)
- 🔄 **Автообновление**: Каждые 2 часа
- 🤖 **Локальный AI**: Работает с Qwen 8B через LM Studio
- 📊 **Микросервисы**: Разделение на сервисы с/без интернета
- 🔍 **RAG система**: Векторный поиск + ключевые слова

## 📰 Просмотр новостей в базе

```bash
# Последние 20 новостей
python3 view_news.py

# Последние 50 новостей
python3 view_news.py --latest 50

# Поиск по ключевому слову
python3 view_news.py --search "санкции"

# Фильтр по источнику
python3 view_news.py --source "РБК"

# Детали конкретной новости
python3 view_news.py --id 964
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip3 install fastapi uvicorn requests feedparser beautifulsoup4 numpy pydantic
```

### 2. Запуск LM Studio

- Откройте LM Studio
- Загрузите модель Qwen 8B (или Qwen 2.5)
- Нажмите "Start Server" (порт 1234)

### 3. Запуск сервисов

```bash
cd /Users/david/bank_news_agent
./start_services.sh
```

### 4. Использование

**Веб-интерфейс (самый удобный!)** 🌐

```bash
# Откройте в браузере
open news_viewer.html
```

Или просто откройте файл `news_viewer.html` двойным кликом!

**Python клиент:**

```bash
# Python клиент
python3 client_example.py "санкции против России"

# Интерактивный режим
python3 client_example.py

# Через curl
curl -X POST http://localhost:8002/ask \
  -H 'Content-Type: application/json' \
  -d '{"question": "решения ЦБ по ключевой ставке", "top_k": 15}'
```

### 5. Остановка

```bash
./stop_services.sh
```

## 📚 Документация

- **[MICROSERVICES.md](MICROSERVICES.md)** - Полная документация по архитектуре
- **[БАНКОВСКАЯ_ВЕРСИЯ.md](БАНКОВСКАЯ_ВЕРСИЯ.md)** - Банковская приоритезация
- **[FILES_OVERVIEW.md](FILES_OVERVIEW.md)** - Обзор файлов

## 📊 Архитектура

```
┌─────────────────────────────────────────┐
│  Python Client / curl                   │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│  Service 2: AI Agent (порт 8002)        │
│  - БЕЗ интернета                        │
│  - Запросы к LLM (Qwen 8B)              │
│  - Генерация ответов                    │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│  Service 1: News Collector (порт 8001)  │
│  - С интернетом                         │
│  - RSS загрузка                         │
│  - Создание embeddings                  │
│  - Банковская приоритезация             │
└───────────────┬─────────────────────────┘
                │
                ▼
┌─────────────────────────────────────────┐
│  SQLite + Vector Embeddings             │
│  - 776+ новостей                        │
│  - 384-мерные векторы                   │
└─────────────────────────────────────────┘
```

## 🎯 Примеры запросов для банка

```bash
# Санкции (КРИТИЧНО!)
python3 client_example.py "санкции против Лукойла и Роснефти"
python3 client_example.py "американские санкции против России"

# ЦБ и монетарная политика
python3 client_example.py "решения ЦБ по ключевой ставке"
python3 client_example.py "инфляция и прогноз ЦБ"

# Валютные риски
python3 client_example.py "курс рубля и валютные риски"
python3 client_example.py "доллар евро динамика"

# Кредитные риски
python3 client_example.py "банкротства крупных компаний"
python3 client_example.py "дефолты и кредитные потери"
```

## 🔧 Конфигурация

### Добавить источники новостей

Отредактируйте `news_sources.json`:

```json
{
  "sources": [
    {
      "name": "Ваш источник",
      "url": "https://example.com/rss",
      "category": "finance",
      "priority": "critical",
      "enabled": true
    }
  ]
}
```

### Настроить ключевые слова

Отредактируйте `news_sources.json` → `bank_keywords`:

```json
{
  "bank_keywords": {
    "critical": ["санкции", "ЦБ", "валюта", "ваше-слово"],
    "high": ["экономика", "рынок"],
    "exclude": ["спорт", "шоу-бизнес"]
  }
}
```

Затем обновите:
```bash
curl -X POST http://localhost:8001/update
```

## 📡 API Endpoints

### Service 1 (порт 8001)

- `GET /health` - Проверка здоровья
- `GET /stats` - Статистика базы
- `POST /search` - Поиск новостей
- `POST /update` - Обновление вручную

### Service 2 (порт 8002)

- `GET /health` - Проверка здоровья
- `POST /ask` - Задать вопрос агенту
- `GET /collector/stats` - Статистика коллектора

Swagger UI:
- http://localhost:8001/docs
- http://localhost:8002/docs

## 🛠️ Устранение проблем

### Сервисы не запускаются

```bash
# Проверьте логи
cat news_collector.log
cat ai_agent.log

# Проверьте LM Studio
curl http://localhost:1234/v1/models

# Проверьте порты
lsof -i :8001
lsof -i :8002
```

### Пустая база

```bash
# Обновите вручную
curl -X POST http://localhost:8001/update
```

## 📈 Текущее состояние

- **Новостей в базе**: 776+
- **Источников RSS**: 10
- **Категории**: finance, economy, banking, politics
- **Приоритезация**: ✅ Включена

## ✅ Преимущества

- ✅ Автоматически находит финансово-критичные новости
- ✅ Исключает нерелевантные темы (спорт, шоу-бизнес)
- ✅ Бустит санкции, ЦБ, валюту (x3)
- ✅ Показывает конкретные цифры и факты
- ✅ Специализированные финансовые источники
- ✅ Микросервисная архитектура для безопасности

## 📞 Помощь

Полная документация в [MICROSERVICES.md](MICROSERVICES.md)

---

Теперь санкции против Лукойла и Роснефти точно не пропустите! 🎯
