# 📊 Learning to Rank - Инструкция по разметке

## Быстрый старт

### Вариант А: Генерация готового датасета (опционально)
```bash
python3 ltr_dataset_generator.py
```

Это создаст файл `ltr_dataset.json` с парами (запрос, новость) и всеми фичами.

### Вариант Б: Добавление своих запросов (НОВОЕ! ⭐)
1. Запустите сервис: `python3 news_collector_service.py`
2. Откройте `ltr_annotator.html` в браузере
3. Введите свой запрос в поле "Добавить свой запрос"
4. Выберите количество кандидатов (топ-10 рекомендуется)
5. Нажмите "🔍 Найти и добавить"
6. Кандидаты с фичами сгенерируются автоматически!

### Шаг 2: Разметка
1. Откройте `ltr_annotator.html` в браузере
2. **Либо** загрузите готовый датасет, **либо** добавляйте свои запросы
3. Размечайте каждую пару:
   - **0** ❌ - Нерелевантна (совсем не подходит)
   - **1** 🤔 - Слабо связана (косвенная связь)
   - **2** ✅ - Релевантна (хороший результат)
   - **3** ⭐ - Идеально (лучший ответ на запрос)

### Шаг 3: Сохранение
- **Автосохранение**: прогресс сохраняется в браузере автоматически
- **💾 Сохранить прогресс**: скачать JSON с разметкой
- **📥 Экспортировать CSV**: готовый датасет для обучения

## Горячие клавиши

- **0-3**: Быстрая оценка релевантности
- **← →**: Навигация между примерами
- **Ctrl+S / Cmd+S**: Сохранить прогресс

## Фичи в датасете

Для каждой пары (запрос, новость) вычисляются:

1. **embedding_score** - векторное сходство (BGE-M3)
2. **bm25_score** - текстовое сходство
3. **ner_overlap** - совпадение именованных сущностей
4. **morpho_match** - морфологическое совпадение слов
5. **title_match** - совпадение с заголовком
6. **exact_match** - точное совпадение запроса
7. **days_ago** - свежесть новости (дней назад)
8. **source_authority** - авторитетность источника
9. **text_length** - длина текста

## Рекомендации по разметке

### ⭐ Оценка 3 (Идеально)
- Новость **напрямую** отвечает на запрос
- Содержит все ключевые слова
- Актуальная и релевантная

**Пример:**
- Запрос: "ЦБ повысил ставку"
- Новость: "Центробанк РФ повысил ключевую ставку до 18%"

### ✅ Оценка 2 (Релевантна)
- Новость связана с темой запроса
- Может быть полезна пользователю
- Но не самый точный ответ

**Пример:**
- Запрос: "ЦБ повысил ставку"
- Новость: "Эксперты прогнозируют рост ставки ЦБ в декабре"

### 🤔 Оценка 1 (Слабо связана)
- Новость затрагивает тему, но косвенно
- Упоминает сущности из запроса в другом контексте

**Пример:**
- Запрос: "ЦБ повысил ставку"
- Новость: "Сбербанк снизил ставки по вкладам после решения ЦБ"

### ❌ Оценка 0 (Нерелевантна)
- Новость не связана с запросом
- Не будет полезна пользователю

**Пример:**
- Запрос: "ЦБ повысил ставку"
- Новость: "Курс доллара вырос до 95 рублей"

## Следующие шаги

После разметки:
1. Экспортируйте CSV через кнопку "📥 Экспортировать CSV"
2. Обучите модель LightGBM/XGBoost на этих данных
3. Интегрируйте модель в поисковую систему

## Статистика

Рекомендуемый минимум для обучения:
- **100+ запросов**
- **2000+ размеченных пар**
- Хорошее распределение по оценкам (не только 3 и 0)

## ❓ FAQ: Топ-10 vs Топ-20 vs Топ-K

### Сколько кандидатов генерировать для каждого запроса?

**Короткий ответ: Топ-10 вполне достаточно!**

**Подробнее:**
- **Топ-10** 👍
  - ✅ Быстрее размечать
  - ✅ Меньше скуки
  - ✅ Достаточно для обучения если базовый поиск неплохой
  - ✅ **Рекомендуется по умолчанию**

- **Топ-15** 👌
  - Золотая середина
  - Больше разнообразия в негативных примерах

- **Топ-20** ⚠️
  - Нужен только если базовый поиск плохой (много мусора в топе)
  - Дольше размечать
  - Больше скуки

**Когда использовать что:**
- У вас **BGE-M3 + NER** → базовый поиск уже хорош → **Топ-10 достаточно**
- Базовый поиск **BM25-only** → в топе много мусора → **Топ-15 или Топ-20**

**Главное** - не количество кандидатов, а:
1. Разнообразие запросов (100+ разных)
2. Качественная разметка
3. Хорошее распределение оценок (0, 1, 2, 3)
