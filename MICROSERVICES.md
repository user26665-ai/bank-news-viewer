# 🏦 Банковский новостной агент - Микросервисная архитектура

## 📐 Архитектура

Система разделена на **2 независимых сервиса**:

### Service 1: News Collector (С интернетом)
**Файл:** `news_collector_service.py`
**Порт:** 8001
**Функции:**
- ✅ Загружает RSS из 10+ источников
- ✅ Создает векторные embeddings через LM Studio
- ✅ Хранит в SQLite с приоритезацией для банка
- ✅ Предоставляет REST API для поиска
- ✅ Автообновление каждые 2 часа

**Зависимости:**
- Интернет (для RSS)
- LM Studio (для embeddings)
- SQLite

### Service 2: AI Agent (БЕЗ интернета)
**Файл:** `ai_agent_service.py`
**Порт:** 8002
**Функции:**
- ✅ Получает новости от Service 1 через API
- ✅ Отправляет запросы к локальному LLM (Qwen 8B)
- ✅ Генерирует ответы с фокусом на финансы
- ✅ Предоставляет REST API для вопросов

**Зависимости:**
- Service 1 API (localhost:8001)
- LM Studio (для LLM inference)

---

## 🚀 Быстрый старт

### 1. Запуск LM Studio

```bash
# Откройте LM Studio
# Загрузите модель Qwen 8B (или Qwen 2.5)
# Нажмите "Start Server"
# Убедитесь что порт 1234
```

### 2. Запуск сервисов

```bash
# Сделайте скрипты исполняемыми (один раз)
chmod +x start_services.sh stop_services.sh test_services.sh

# Запустите оба сервиса
./start_services.sh
```

Вы увидите:
```
✓ LM Studio доступен
✓ Service 1 запущен
✓ Service 2 запущен

✅ Оба сервиса успешно запущены!
```

### 3. Тестирование

```bash
./test_services.sh
```

### 4. Использование

**Через Python клиент (рекомендуется):**
```bash
python3 client_example.py "санкции против России"
```

**Через curl:**
```bash
curl -X POST http://localhost:8002/ask \
  -H 'Content-Type: application/json' \
  -d '{"question": "решения ЦБ по ключевой ставке", "top_k": 15}'
```

### 5. Остановка

```bash
./stop_services.sh
```

---

## 📡 API Reference

### Service 1: News Collector (порт 8001)

#### `GET /health`
Проверка здоровья сервиса

```bash
curl http://localhost:8001/health
```

#### `GET /stats`
Статистика базы новостей

```bash
curl http://localhost:8001/stats
```

Ответ:
```json
{
  "total_news": 776,
  "by_source": {
    "РБК - Финансы": 150,
    "Ведомости": 120,
    ...
  },
  "by_category": {
    "finance": 400,
    "economy": 300,
    ...
  },
  "last_update": "2025-10-27T10:30:00"
}
```

#### `POST /search`
Поиск новостей (гибридный: текст + векторный + банковская приоритезация)

**Запрос:**
```json
{
  "query": "санкции против России",
  "top_k": 20
}
```

**Ответ:**
```json
{
  "query": "санкции против России",
  "total_found": 15,
  "news": [
    {
      "id": 123,
      "title": "США разрабатывают новые санкции против России",
      "description": "...",
      "source": "Reuters",
      "published": "2025-10-25",
      "similarity": 3.28,
      "critical_keywords": 1,
      "keyword_score": 0.85,
      "vector_score": 0.92,
      "bank_boost": 3.5
    },
    ...
  ],
  "timestamp": "2025-10-27T10:30:00"
}
```

#### `POST /update`
Запустить обновление новостей вручную

```bash
curl -X POST http://localhost:8001/update
```

---

### Service 2: AI Agent (порт 8002)

#### `GET /health`
Проверка здоровья сервиса и зависимостей

```bash
curl http://localhost:8002/health
```

Ответ:
```json
{
  "service": "healthy",
  "dependencies": {
    "news_collector": "healthy",
    "llm": "healthy"
  }
}
```

#### `POST /ask`
Задать вопрос агенту (основной endpoint)

**Запрос:**
```json
{
  "question": "санкции против России",
  "top_k": 15
}
```

**Ответ:**
```json
{
  "question": "санкции против России",
  "answer": "⚠️ Санкции против РФ:\n1. Reuters: США разрабатывают новые санкции (25 окт.)\n2. Трамп ввел санкции из-за Рубио (24 окт.)\n3. МосБиржа и РТС упали на 1,9%\n4. Усиление давления на финансовый сектор",
  "news_found": 15,
  "top_news": [
    {
      "title": "США разрабатывают новые санкции против России",
      "description": "...",
      "source": "Reuters",
      "published": "2025-10-25",
      "similarity": 3.28,
      "critical_keywords": 1
    },
    ...
  ],
  "timestamp": "2025-10-27T10:30:00"
}
```

#### `GET /collector/stats`
Получить статистику от News Collector (прокси)

```bash
curl http://localhost:8002/collector/stats
```

---

## 🔧 Конфигурация

### Изменить интервал обновления новостей

Отредактируйте `news_collector_service.py`:

```python
UPDATE_INTERVAL_SECONDS = 7200  # 2 часа (по умолчанию)
```

### Добавить источники новостей

Отредактируйте `news_sources.json`:

```json
{
  "sources": [
    {
      "name": "Ваш источник",
      "url": "https://example.com/rss",
      "category": "finance",
      "priority": "critical",
      "enabled": true
    }
  ]
}
```

### Настроить ключевые слова для банка

Отредактируйте `news_sources.json`:

```json
{
  "bank_keywords": {
    "critical": [
      "санкции",
      "ЦБ",
      "валюта",
      "ваше-ключевое-слово"
    ],
    "high": [
      "экономика",
      "рынок"
    ],
    "exclude": [
      "спорт",
      "шоу-бизнес"
    ]
  }
}
```

Затем обновите базу:
```bash
curl -X POST http://localhost:8001/update
```

---

## 📊 Мониторинг

### Логи

```bash
# Service 1
tail -f news_collector.log

# Service 2
tail -f ai_agent.log
```

### Статус сервисов

```bash
# Проверить что сервисы запущены
ps aux | grep "collector_service\|agent_service"

# Проверить порты
lsof -i :8001
lsof -i :8002
```

### Swagger UI документация

- Service 1: http://localhost:8001/docs
- Service 2: http://localhost:8002/docs

---

## 🎯 Примеры использования

### 1. Python клиент (интерактивный режим)

```bash
python3 client_example.py
```

Затем вводите вопросы:
```
💬 Ваш вопрос: санкции против России
💬 Ваш вопрос: решения ЦБ
💬 Ваш вопрос: exit
```

### 2. Python клиент (одна команда)

```bash
python3 client_example.py "санкции против Лукойла и Роснефти"
```

### 3. Curl запросы

**Прямой поиск (без AI):**
```bash
curl -X POST http://localhost:8001/search \
  -H 'Content-Type: application/json' \
  -d '{"query": "валютные риски", "top_k": 10}'
```

**С AI агентом:**
```bash
curl -X POST http://localhost:8002/ask \
  -H 'Content-Type: application/json' \
  -d '{"question": "курс рубля и прогнозы", "top_k": 15}'
```

### 4. Python скрипт

```python
import requests

# Задать вопрос агенту
response = requests.post(
    "http://localhost:8002/ask",
    json={"question": "санкции против России", "top_k": 15}
)

result = response.json()
print(f"Найдено: {result['news_found']} новостей")
print(f"Ответ: {result['answer']}")
```

---

## 🔥 Важные банковские запросы

```bash
# Санкции (критично!)
python3 client_example.py "санкции против России"
python3 client_example.py "санкции против Лукойла и Роснефти"

# ЦБ и монетарная политика
python3 client_example.py "решения ЦБ по ключевой ставке"
python3 client_example.py "инфляция и прогноз ЦБ"

# Валютные риски
python3 client_example.py "курс рубля и валютные риски"
python3 client_example.py "доллар евро динамика"

# Кредитные риски
python3 client_example.py "банкротства крупных компаний"
python3 client_example.py "дефолты и кредитные потери"

# Конкретные компании
python3 client_example.py "Сбербанк ВТБ финансовые результаты"
python3 client_example.py "Газпром Лукойл корпоративные новости"
```

---

## 🆚 Преимущества микросервисной архитектуры

| Аспект | Монолит | Микросервисы |
|--------|---------|--------------|
| Интернет доступ | Везде или нигде | Только в Service 1 |
| Масштабирование | Все вместе | Независимо |
| Отказоустойчивость | Падает все | Изолированные сбои |
| Обновление | Перезапуск всего | Перезапуск одного сервиса |
| Безопасность | Общая зона | Service 2 изолирован от интернета |
| Нагрузка | Смешана | Распределена |

**Для банка критично:**
- ✅ Service 2 (AI Agent) полностью изолирован от интернета
- ✅ Embeddings создаются в Service 1 (с интернетом)
- ✅ Можно масштабировать Service 2 на несколько инстансов
- ✅ Можно обновлять новости без остановки AI агента

---

## 🛠️ Troubleshooting

### Service 1 не запускается

```bash
# Проверьте логи
cat news_collector.log

# Проверьте что порт свободен
lsof -i :8001

# Проверьте LM Studio
curl http://localhost:1234/v1/models
```

### Service 2 не видит Service 1

```bash
# Проверьте что Service 1 запущен
curl http://localhost:8001/health

# Проверьте connectivity
curl http://localhost:8001/stats
```

### LLM не отвечает

```bash
# Проверьте LM Studio
curl http://localhost:1234/v1/models

# Проверьте что модель загружена в LM Studio UI
# Убедитесь что сервер запущен
```

### Пустая база новостей

```bash
# Запустите обновление вручную
curl -X POST http://localhost:8001/update

# Или из консоли
python3 -c "from news_rag_system import NewsRAGSystem; rag = NewsRAGSystem(); print(rag.fetch_and_index_news())"
```

---

## 📈 Производительность

### Типичные времена ответа:

- **Поиск новостей (Service 1):** 50-200ms
- **AI ответ (Service 2):** 5-30 секунд (зависит от Qwen 8B)
- **Обновление RSS:** 30-60 секунд (10 источников)
- **Создание embedding:** 100-300ms на новость

### Рекомендации:

- **RAM:** Минимум 16GB (Qwen 8B требует ~8GB)
- **CPU:** 4+ ядер
- **Диск:** SSD для SQLite
- **База:** До 10,000 новостей без проблем

---

## ✅ Итого

**Микросервисная архитектура:**
- ✅ Service 1 (с интернетом): RSS + embeddings + API
- ✅ Service 2 (без интернета): AI agent + LLM
- ✅ Автоматическое обновление каждые 2 часа
- ✅ Банковская приоритезация (x3 для критичных новостей)
- ✅ REST API для интеграции
- ✅ Python клиент для удобства

**Используйте:**
```bash
# Запуск
./start_services.sh

# Использование
python3 client_example.py "ваш вопрос"

# Остановка
./stop_services.sh
```

Теперь санкции против Лукойла точно не пропустите! 🎯
